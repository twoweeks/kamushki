export type TwitchAuthResponseType = {
    access_token: string;
    expires_in: number;
    token_type: string;
};

export type TwitchTokenFormatInFSType = {
    access_token: string;
    expires_date: number;
};
